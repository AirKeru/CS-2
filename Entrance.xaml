<Window x:Class="CS_1.Entrance"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CS_1"
        mc:Ignorable="d"
        Title="CS-2" Height="300" Width="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <PasswordBox Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10" Width="200" Name="Password"/>
        <Button Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10" Width="100" Height="20" Name="PasswordVerificationBtn" Click="PasswordVerificationBtn_Click" Content="Вход"/>
        <Button Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10" Width="100" Height="20" Name="ExitBtn" Click="ExitBtn_Click" Content="Выход"/>
        <TextBlock Text="CS-2" VerticalAlignment="Top" HorizontalAlignment="Center"  FontSize="40" Margin="15"/>
        <TextBlock Text="Ведите пароль" VerticalAlignment="Bottom" HorizontalAlignment="Center" FontSize="14" Margin="30"/>
    </Grid>
</Window>
